# 待办：OpenCode 快捷键支持

> 创建：2026-08-26
> 状态：待办（明天继续）

## 需求

用户主要使用 **OpenCode**，需要手柄快捷键支持 OpenCode 的斜杠命令操作。

## 具体想法

1. **一个快捷键能打出 `/`**：打开 OpenCode 命令菜单，然后手动选择/输入命令
2. **常用斜杠命令直接绑定**（可选进阶）：
   - `/new` — 新对话
   - `/sessions` — 会话列表 / 切换对话
   - `/compact` — 压缩上下文
   - `/models` — 切换模型
   - `/agents` — 切换 agent
   - `/undo` / `/redo` — 撤销 / 重做
   - `/help` — 帮助
   - `/init` — 初始化项目
   - `/config` — 配置
   - （明天实测确认 OpenCode 当前版本的实际命令列表）

## 候选按键（当前未绑定）

- **Back**（未绑定）
- **GUIDE**（未绑定）
- **LT 修饰层扩展**（LT+Back / LT+GUIDE 等）
- 或 **LB 修饰层**（LB 当前是 Ctrl+C，可考虑 LB+某键）

## 明天步骤

1. 确认 OpenCode 的斜杠命令列表（运行 opencode 后按 `/` 查看）
2. 设计按键绑定方案（先做"打出 /"的快捷键，再考虑常用命令直绑）
3. 改 `config.json`
4. 实测：手柄按键 → 打出 `/` → 命令菜单出现 → 选择命令
5. 更新 README / 桌面映射表 / memory
6. 推送 GitHub